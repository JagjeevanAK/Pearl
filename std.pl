use Modern::Perl;

# STDIN, STDOUT, STDERR

# STDIN is use to take a input from the keyboard

my $input = <STDIN>;
print $input;

# OR it can read input from file as well
my $input2 = <>;
print $input2;